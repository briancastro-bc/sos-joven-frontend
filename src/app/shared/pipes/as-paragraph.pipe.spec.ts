import { AsParagraphPipe } from './as-paragraph.pipe';

describe('AsParagraphPipe', () => {
  it('create an instance', () => {
    const pipe = new AsParagraphPipe();
    expect(pipe).toBeTruthy();
  });
});
